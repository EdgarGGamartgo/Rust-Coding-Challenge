[package]
name = "code_challenge_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
serde_json = "1.0"
codegen_proc_macro = { path = "codegen_proc_macro" }

[build-dependencies]
codegen_proc_macro = { path = "codegen_proc_macro" }